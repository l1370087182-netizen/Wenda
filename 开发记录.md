# 开发记录

> 按模块/步骤记录每一步做了什么、关键文件、验证结果。架构依据：《六类资讯日报系统-架构设计文档.md》

---

## 步骤1：项目骨架（依赖 / 目录 / config / 入口）✅

**做了什么：**
- `uv add` 安装全部依赖：fastapi、uvicorn、sqlalchemy(async)、asyncpg、pgvector、alembic、apscheduler、redis、aiosmtplib、pydantic-settings、bcrypt、httpx、feedparser、beautifulsoup4、lxml、jinja2、openai、langgraph、langgraph-checkpoint-redis、email-validator
- 删除脚手架 hello `main.py`，创建 `app/` 包结构：`api/`、`core/`、`services/collectors/`、`static/`
- `app/config.py`：pydantic-settings 全量配置（管理员、会话、定时任务时间、SMTP、验证码限流、LLM 双模型、Embedding、存储连接、RAG 参数）
- `app/main.py`：FastAPI 入口 + lifespan（建表 → 管理员引导 → 启动/停止调度器）+ `/api/health`
- `app/services/db.py`：async 引擎 + session 工厂 + `init_db()`（开发期 create_all）

**验证：** 全部依赖导入 OK；`uv run python -c "from app.main import app"` 通过

---

## 步骤2：数据模型（9 张表 + pgvector）✅

**做了什么：**
- `app/models.py`：SQLAlchemy 2.0 声明式模型——
  - `users`（role 枚举、订阅分类 JSONB、last_login_at/last_used_at）
  - `email_codes`（purpose: register/reset、过期、已用标记）
  - `sessions`（**只存 token 的 sha256**，不存明文）
  - `articles`（url_hash 唯一索引、pgvector `Vector(dim)` embedding、(category, batch_date) 复合索引）
  - `digests`（category 含 `insight` 行；(category, date) 唯一）
  - `send_logs`（digest_date + categories JSONB，配合"每天一封邮件"）
  - `job_runs`（running/success/partial/failed + error，支撑失败兜底提示）
  - `chats` + `chat_messages`（多轮会话，消息带 sources JSONB）
- 工具函数：`new_token()` / `hash_token()` / `utcnow()`

**验证：** `Base.metadata.tables` 共 9 张，导入 OK；架构文档表数已同步为 9

---

## 步骤3：core 模块（security / email / deps）✅

**做了什么：**
- `app/core/security.py`：bcrypt 密码哈希/校验；会话创建（明文 token 只进 Cookie，库里存 sha256）；Cookie 设置（HttpOnly + SameSite=Lax，secure 生产开启）；过期会话清理
- `app/core/email.py`：SMTP 发信（465 SSL / 587 STARTTLS 自适应，线程池 + 信号量限并发）；验证码 6 位生成、**同邮箱同用途 60s 限流**（PG 查询实现）、过期校验、一次性使用标记
- `app/core/deps.py`：`get_db` / `get_current_user`（Cookie→hash 查会话→校验过期）/ `require_admin` / `touch_activity`（last_used_at 节流 1 分钟写一次）

**验证：** `from app.core import security, email, deps` + `from app.main import app` 通过

---

## 步骤4：Auth API（注册 / 登录 / 找回 / 会话）✅

**做了什么：**
- `app/api/auth.py` 六个接口：
  - `POST /api/auth/send-code`（register/reset 双用途，频率超限返回 429）
  - `POST /api/auth/register`（验证码校验→建号→Set-Cookie）
  - `POST /api/auth/login`（密码校验→建会话→更新 last_login_at）
  - `POST /api/auth/logout`（销毁服务端会话 + 清 Cookie）
  - `POST /api/auth/reset-password`（验证码→改密）
  - `GET /api/auth/me`（当前用户 + 设置，顺带更新活跃度）
- `ensure_admin(db)`：启动时按 .env 创建/同步管理员（唯一修改途径，不走注册/找回）
- `main.py` 挂载 auth router，lifespan 中调用 ensure_admin

**验证：** TestClient 冒烟——`POST /api/auth/login` 路由命中（500 = 本机无数据库，预期），`GET /api/auth/me` 无 Cookie 正确返回 401

**基础设施：**
- `docker-compose.dev.yml`：本地开发用 postgres(pgvector/pgvector:pg17) + redis，app 跑宿主机
- `README 环境变量清单`：全量环境变量模板（管理员密码留空，不写真实密码）

**⚠️ 待办：** 启动 Docker Desktop → `docker compose -f docker-compose.dev.yml up -d` 后做完整注册/登录链路集成测试

---

## 步骤5：Users / Admin API ✅

**做了什么：**
- `app/api/users.py`：`PUT /api/users/me/settings`（notify_enabled + subscribed_categories，非法分类 400 校验）
- `app/api/admin.py`：`GET /api/admin/users`（分页、date_after 过滤，返回注册时间/上次使用/登录时间；`require_admin` 依赖守卫）
- `main.py` 挂载两个 router

**验证：** 全模块导入 OK

---

## 步骤6：Dashboard / Chat API ✅

**做了什么：**
- `app/api/dashboard.py`：
  - `GET /api/dashboard/top?date=&category=`——单分类直接查询；全六类用**窗口函数**一次 SQL 按 (importance, hot) 排名取各分类 TopN
  - `GET /api/dashboard/insight?date=`——返回当日交叉洞察（digests 中 category=insight 行）
  - `GET /api/dashboard/articles/{id}`——详情（含 impact/content）
- `app/api/chat.py`：
  - `POST /api/chat`——chat_id 续聊/新建会话（标题=问题前 30 字）、历史取最近 10 条、用户消息落库、调 `services.graph.run_chat`（步骤8实现，懒导入）、助手消息带 sources 落库、编排异常兜底文案
  - `GET /api/chats` / `GET /api/chats/{id}`——历史会话与消息记录（校验归属）

**验证：** 全 router 导入 OK

---

## 步骤7：LLM 封装 + pgvector 向量层 + 六类采集器 ✅

**做了什么：**
- `app/services/llm.py`：AsyncOpenAI 兼容客户端；`chat()` 模型分层（fast/strong）；`chat_json()` 强制 JSON（失败重试一次）；`embed_texts()` 批量向量化；**全局 `asyncio.Semaphore(8)` 并发闸门**
- `app/services/vector.py`：
  - `find_duplicates()`——url_hash 精确去重 + pgvector 余弦距离语义去重（阈值 0.95）
  - `search()`——查询向量化 → 余弦距离排序 → RAG 检索（带日期窗口/分类过滤）
- `app/services/collectors/`：
  - `base.py`：异步 fetch（失败返回 None）、RSS 解析（feedparser）、HTML 清洗、url_hash、`collect_rss()` 多源并行抓取并返回 `(items, failed_sources)`
  - 六类采集器：`tech.py`（36氪+TechCrunch）、`geo.py`（Google News 地缘×3 查询）、`finance.py`（华尔街见闻+CNBC）、`ai_tech.py`（arXiv cs.AI/cs.CL）、`ai_news.py`（机器之心+Google News AI）、`github.py`（GitHub Search API 近 7 天 star 榜）
  - 统一返回 `tuple[list[dict], list[str]]`——**失败源清单供采集 Agent 换源决策和 job_runs 健康报告**

**验证：** 全模块导入 OK；修复 pgvector 距离 API（`embedding.cosine_distance()` 列方法）

---

## 步骤8：LangGraph 多 Agent 编排 ✅

**做了什么：**
- `app/services/graph.py`（核心编排，8 节点 StateGraph）：
  - **采集 Agent ×6 并行**：主源直通不调 LLM；失败/条数不足 5 条时启用**换源决策**（fast 模型生成搜索词 → Google News RSS 补源），受时间预算（08:00 前 15 分钟）约束；产出 raw_items + 健康报告
  - **去重入库**：批量向量化（LLM 未配置时优雅降级为 url_hash 去重）→ url_hash 精确 + pgvector 余弦语义去重 → 入库
  - **分析 Agent**：每篇一次 function calling 结构化输出（hot/importance/summary/impact），较三段串行降 2/3 调用
  - **digest 生成**：各分类 Top5 渲染草稿（pg upsert 到 digests）
  - **交叉洞察 Agent**：每类 Top3 摘要 → strong 模型一次调用 → 1-3 条跨类关联链 → insight 行
  - **主编审查**：strong 模型审草稿（矛盾/空泛/评分不符）→ 条件路由到**修订 Agent**（≤2 轮反思循环）→ finalize
  - `finalize`：按 6 类健康报告汇聚 job_runs 状态（success/partial/failed）
- **研究问答**：`run_chat()` 路由器（factual→单轮快速通道 / analysis→研究 Agent）；研究 Agent：拆解子问题→并行检索→证据自检（不足补检索 1 轮）→带编号引用综合作答
- **checkpointer**：`AsyncRedisSaver`（断点续跑，thread_id=collect-{date}），不可用退化 MemorySaver；state 只存可序列化 JSON，db 会话走 configurable
- `app/services/digest.py`：单分类 digest 渲染 / 洞察渲染 / **每天一封合并邮件**（订阅分类分节 + 今日关联板块 + **失败友好提示**：缺席原因+建议操作）

**验证：** 图构建 8 节点齐全、无警告；graph/digest 导入 OK

---

## 步骤9：定时任务 + 发送 + 失败兜底 ✅

**做了什么：**
- `app/services/scheduler.py`（AsyncIOScheduler，显式 Asia/Shanghai 时区）：
  - `job_collect_daily`（07:00）→ `graph.run_daily`
  - `job_send_daily`（08:00）→ 活跃用户（7 天内登录过）+ notify_enabled 过滤 → 订阅分类合并**一封邮件**（含今日关联、缺席提示）→ SMTP 指数退避重试 3 次 → send_logs 落库 → digest 标记 sent
  - `job_cleanup`（03:30）→ 过期会话清理
- `app/api/tasks.py`：
  - `GET /api/tasks/status`——最近 N 天 job_runs 状态（登录即可查）
  - `POST /api/tasks/run`——**管理员手动重跑**采集流水线
  - `POST /api/tasks/send`——**管理员手动补发**当日日报（失败兜底的"解决"动作）
- `main.py` 挂载 tasks router

**验证：** 路由表 18 条全部注册，与架构文档 API 清单一致

---

## 步骤10：前端单页 ✅

**做了什么：**
- `app/static/index.html`：三视图单页（认证 / 主应用三 Tab）
- `app/static/style.css`：全手写样式（看板栅格、聊天气泡、响应式）
- `app/static/app.js`（原生 JS，无构建链）：
  - 认证：登录/注册/找回三 Tab，验证码按钮 60s 冷却
  - 看板：日期选择 → 六类 Top5 ECharts 横向条形图（重要度/热度）+ 文章卡片列表 + 今日关联洞察框 + 采集状态标签（success/partial/failed）
  - 问答：会话侧栏（历史/新建）+ 多轮气泡对话 + 来源编号引用链接
  - 设置：通知开关 + 六类订阅勾选 → PUT settings
- ECharts 走 CDN，FastAPI 直接托管 static（html=True），无 Node 构建链

**验证：** 无（静态资源，浏览器访问验证）

---

## 步骤11：部署配置 ✅

**做了什么：**
- `Dockerfile`：uv 官方镜像（python3.13-slim）分层构建；**CMD 钉死 `--workers 1`**（APScheduler 多进程会重复触发任务）
- `docker-compose.yml`（生产）：app(mem 1g) + postgres/pgvector:pg17(mem 320m, shared_buffers=128MB) + redis(mem 64m, allkeys-lru)，healthcheck + restart 策略
- `docker-compose.dev.yml`（本地开发）：仅 postgres + redis，app 跑宿主机
- `README 环境变量清单`：全量模板（管理员密码留空不写真实值）
- `README.md`：开发/部署说明（含 2G swap 配置）、定时任务表、失败兜底说明、API 一览
- `.gitignore` 增加 `.env`

---

## 集成冒烟测试 ✅（2026-09-04）

**环境准备：**
- 启动 Docker Desktop；原 daemon.json 中三个镜像加速器全部失效（163/ustc/baidubce），替换为 docker.1ms.run + docker.m.daocloud.io（原配置备份于 `daemon.json.bak`）
- `docker compose -f docker-compose.dev.yml up -d` → postgres(pgvector:pg17) + redis 就绪
- `.env` 从模板创建，管理员密码设开发值（SMTP/LLM 留空 = 降级模式）

**发现并修复的问题（3 个）：**
1. `services/db.py`：`base.Base` 残留引用 → 改为 `models.Base`
2. **pgvector 扩展未启用**：`create_all` 前需 `CREATE EXTENSION IF NOT EXISTS vector`（已加进 init_db，生产镜像同样适用）
3. `scheduler.py` 导入路径错误（`app.services.email` → `app.core.email`）
4. `auth.py` 登录用 `EmailStr` 拦截了管理员用户名 `adminljj` → 登录接口放宽为普通字符串（注册/找回仍严格校验）
5. `dashboard.py` 窗口函数子查询取值方式错误（`scalars` 取到标量）→ 改用 `aliased(Article, subq)`；同时修正 `sqlalchemy.orm.aliased` 导入位置

**验证结果：**
- ✅ 应用启动：建表 9 张 + pgvector 扩展 + 管理员自动创建
- ✅ API 链路：health → 登录（Cookie）→ me → PUT settings → admin/users（含注册/登录/使用时间）→ dashboard/top → tasks/status → chats
- ✅ **真实采集流水线**：`POST /api/tasks/run` 端到端 29 秒完成，90 篇文章入库（github 15 / ai_tech 30 / tech 15 / finance 30），4 份 digest 生成
- ✅ 状态汇聚：geo/ai_news 因 Google News 源在本网络不可达 → `partial` + 失败原因写入 job_runs（正好验证了失败兜底链路）
- ✅ 降级模式：无 LLM Key 时 url_hash 去重、评分默认值、摘要截断，流水线不断
- ✅ 前端单页：`GET /` 正常返回

**未验证（需要真实 Key）：** LLM 评分/摘要/洞察/审查、Embedding 语义去重与 RAG 问答、SMTP 验证码与日报发送——待填入 .env 后按同一方式验证

**当前状态：** dev 容器（postgres/redis）保持运行；app 测试完已停止，`uv run uvicorn app.main:app --workers 1` 可随时启动

---

## 步骤12：项目内置 Skill 系统 ✅（2026-09-04）

**做了什么：**
- `app/services/skills.py`：SkillRegistry——扫描 `app/skills/*/SKILL.md`，解析 Anthropic Agent Skills 格式 frontmatter（纯正则解析，无 yaml 依赖）；渐进式披露两级 API（list_skills 只给 name+description / get_skill 命中才加载正文）；缓存扫描结果
- `app/skills/` 3 个技能包（规范格式）：
  - `weekly-report`——深度周报（聚类主线/固定结构/800 字限制）
  - `event-timeline`——事件时间线（锚定主体/多轮检索/诚实原则）
  - `impact-analysis`——影响分析（传导链/事实与推测分开/反方观点/禁止无证据预测）
- `graph.py` 接入研究问答：
  - 路由提示词附技能目录（`_route_messages`），要求返回 skill 字段
  - 命中技能 → 正文注入研究 Agent 的规划提示词 + 综合提示词（按技能输出规范作答）
  - `run_chat` 返回 `skill` 字段，chat API 透传
- `app/api/skills.py`：`GET /api/skills` 已装技能目录（登录用户）
- 架构文档：§4.5 Skill 层章节、§7 API、§8 目录结构、§10 决策 5

**验证：** 注册器扫描出 3 个技能、正文加载正常、app 导入通过（控制台乱码仅为 Windows 编码显示）

**待联调：** LLM Key 填好后验证路由命中（问"本周总结"→ weekly-report）与技能正文注入效果

---

## 步骤13：前后端分离（Vue 3 + Element Plus）✅（2026-09-04）

**做了什么：**
- `frontend/` 独立 Vue 3 + Vite + Element Plus 工程（npm 源走 npmmirror）：
  - 四视图重写：`AuthView`（三 Tab 认证+验证码冷却）、`DashboardView`（日期/采集状态 Tag/洞察卡/ECharts 双图/文章卡片）、`ChatView`（会话侧栏+多轮气泡+**技能命中徽章**+**侧栏已装技能目录**）、`SettingsView`
  - `api.js` axios 封装（401 自动跳登录）；hash 路由；Element Plus 中文 locale
  - Vite dev 代理 `/api` → 8000（开发期免 CORS、Cookie 同源）
- 后端适配：
  - `main.py`：生产挂载 `frontend/dist`（存在才挂，html=True）；新增 CORS 中间件（`CORS_ORIGINS` 配置，默认 localhost:5173，仅开发期需要）
  - 删除旧 `app/static/`（原生 JS 版被 Vue 版取代）
  - `Dockerfile` 多阶段构建：node:20-alpine `npm ci && npm run build` → 产物拷入 Python 运行时，**运行时零 Node 开销、同源免跨域 Cookie 问题**
- `.gitignore`：frontend/node_modules、frontend/dist

**验证：**
- ✅ `npm run build` 11 秒通过（Element Plus+echarts 分包）
- ✅ FastAPI 同源托管 dist：`GET /` 返回 Vue 页面、JS 资源 200
- ✅ 同源 API 链路：登录 → `GET /api/skills` 返回 3 个技能（weekly-report / event-timeline / impact-analysis）
- 架构文档：§2 技术栈、§8 目录、决策 6 已同步；旧"不引入 Node 构建链"决策更新为"Node 仅构建期"

**已知小问题：** DashboardView 的 ECharts 图表在窗口 resize 后不自适应（未挂 resize 监听），后续可加 `chart.resize()` 监听

---

## 步骤14：MCP Server（方向 A）✅（2026-09-04）

**做了什么：**
- `uv add mcp[cli]`（官方 SDK，2.x API——FastMCP 已改名 MCPServer）
- `app/mcp_server.py`：7 个 MCP 工具——只读 5 个（get_top_news / get_cross_insight / search_knowledge / ask / get_pipeline_status）+ 管理 2 个（rerun_pipeline / resend_digest，token 鉴权，`MCP_ADMIN_TOKEN` 留空则禁用）；全部复用现有 services，零业务逻辑改动
- `main.py`：session manager 生命周期手动接管进 lifespan；挂载 `/mcp`

**踩坑记录（3 个，都是 2.x SDK + Starlette 1.6 的新问题）：**
1. 参数名 `date` 遮蔽 `datetime.date` 类 → 统一改 `day_str`
2. 子应用内部路径默认 `/mcp`，挂到 `/mcp` 下会变成 `/mcp/mcp` → `streamable_http_path="/"`
3. **精确路径 `/mcp`（无尾斜杠）被 `/` 静态挂载吞掉返回 405**（Starlette 1.6 Mount 不匹配空余路径）→ 双保险：MCPPathFixMiddleware 把 `/mcp` 重写为 `/mcp/` + ASGI 包装器把空路径规整为 `/`

**验证：** 官方 ClientSession 连接 → initialize/list_tools（7 个）→ 真实调用 get_pipeline_status（返回当日 partial + geo/ai_news 失败详情）、get_top_news、鉴权验证（错误 token 正确拒绝）

**文档：** 架构文档 §4.6 MCP Server 章节、README MCP 接入说明；**推送前安全清理**——架构文档中的真实管理员密码已替换为占位符

---

## 步骤15：LLM 双协议 + 用户自带模型（BYOK）✅（2026-09-04）

**做了什么：**
- `app/services/llm.py` 重构：
  - **双协议适配**：openai（AsyncOpenAI）/ anthropic（httpx 直调 /v1/messages，system 拆独立参数，拼接 content blocks），统一 `chat/chat_json` 入口，`cfg` 参数按调用注入
  - **通用 URL 规则**：`#` 结尾=完整端点原样用；已含默认路径则不重复拼接；否则标准拼接——火山方舟 `/api/v3` 前缀直接可用，任意特殊端点（如 `/api/plan` 系）加 `#` 兼容
  - `LLMConfig` dataclass（provider/base_url/api_key/model_fast/model_strong/label）
  - `test_config()` 连通性测试（返回延迟+错误详情）
- **BYOK**：`user_llm_configs` 表（第 10 张，api_key 只写不回传）+ `app/api/llm_config.py`（GET 回掩码/PUT key 留空保持旧值/DELETE 回退系统/POST test）+ chat API 加载用户配置传入 run_chat → 路由/规划/综合全链路生效
- **关键设计**：Embedding/检索**始终走系统配置**——用户向量必须与索引同模型，否则 pgvector 相似度无意义
- 前端 SettingsView 新增"外部模型"卡片：协议单选、base_url 占位提示含火山示例、key 掩码回显、测试连接（延迟/错误展示）、清除回退
- config 新增 `LLM_PROVIDER`（系统默认也可用 anthropic）

**验证：** URL 规则断言通过（火山 v3/plan/# 三种形态）、llm-config 4 路由注册、前端构建通过

**待联调：** 真实 key 下 anthropic 协议调用与火山端点实测

---



---









## 待办

- [ ] 填入真实 LLM/Embedding Key → 验证语义去重、分析、洞察、审查、问答 + Skill 路由命中
- [ ] 真实 key 下实测 anthropic 协议 + 火山方舟端点（BYOK 测试连接）
- [ ] 填入 SMTP → 验证验证码邮件与日报发送
- [ ] geo/ai_news 换用国内可达源（Google News 在服务器上如不可达会触发换源，但搜索兜底同样是 Google News，建议直接换源）
- [ ] ECharts resize 自适应（前端小问题，见步骤13）
- [ ] 生产环境 alembic 迁移替代 create_all
- [ ] 观察一次完整 07:00/08:00 定时任务实跑
